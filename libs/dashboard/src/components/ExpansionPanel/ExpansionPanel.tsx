/* eslint-disable-next-line */
export interface ExpansionPanelProps {}

export const ExpansionPanel = (props: ExpansionPanelProps) => {
  return (
    <div>
      <h1>Welcome to ExpansionPanel!</h1>
    </div>
  );
};

export default ExpansionPanel;
